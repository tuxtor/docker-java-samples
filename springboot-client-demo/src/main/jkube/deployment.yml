spec:
  template:
    spec:
      containers:
        - env:
          - name: "MANAGEMENT_OTLP_TRACING_ENDPOINT"
            value: "http://simplest-collector.${jkube.namespace}.svc.cluster.local:4318/v1/traces"
          - name: "MANAGEMENT_OTLP_METRICS_EXPORT_URL"
            value: "http://simplest-collector.${jkube.namespace}.svc.cluster.local:4318/v1/metrics"
          - name: "SPRING_CLOUD_OPENFEIGN_CLIENT_CONFIG_ADMBOOKCLIENT_URL"
            value: "http://springboot-demo.${jkube.namespace}.svc.cluster.local:8080"
          resources:
            requests:
              cpu: "1000m"
              memory: "1024Mi"
            limits:
              cpu: "1000m"
              memory: "2048Mi"
          readinessProbe:
            failureThreshold: 2
            successThreshold: 1
            initialDelaySeconds: 60
            httpGet:
              port: 8080
              scheme: HTTP
              path: /actuator/health
          livenessProbe:
            failureThreshold: 3
            successThreshold: 1
            initialDelaySeconds: 120
            httpGet:
              port: 8080
              scheme: HTTP
              path: /actuator/health